/**
 * ENS SDK - Main Export File
 * 
 * Enhanced ENS Resolution & Internal Naming System
 * Supports both real ENS resolution and internal contract/transaction naming
 */

export * from './ens-sdk';

// Re-export for convenience
export { ensSDK as default } from './ens-sdk';
